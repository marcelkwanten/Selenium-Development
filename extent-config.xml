<?xml version="1.0" encoding="UTF-8"?>
<extentreports>
	<configuration>
		<!-- document encoding -->
		<!-- defaults to UTF-8 -->
		<encoding>UTF-8</encoding>

		<!-- report headline - displayed at top-nav, after reportHeadline -->
		<reportHeadline></reportHeadline>

		<!-- global date format override -->
		<!-- defaults to yyyy-MM-dd -->
		<dateFormat>dd-MM-yyyy</dateFormat>

		<!-- global time format override -->
		<!-- defaults to HH:mm:ss -->
		<timeFormat>HH:mm:ss</timeFormat>

		<!-- custom javascript -->
		<scripts>
      <![CDATA[
      $(document).ready(function() {
		
      });
      ]]>
		</scripts>

		<!-- custom styles -->
		<styles>
      <![CDATA[
		.table-results tr:nth-child(odd){ background-color:#eee; }
		.table-results tr:nth-child(even){ background-color:#fff; }
		
		.step-details tr:nth-child(odd){ background-color:transparent; }
		.step-details tr:nth-child(even){ background-color:transparent; }
.test-body .collapsible-body table {
  margin-top:0px;
}
		pre {
		    text-transform: none;
		}

      ]]>
		</styles>
	</configuration>
</extentreports>